{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = MUConnectionToolbarController; 
            LANGUAGE = ObjC; 
            OUTLETS = {window = NSWindow; windowController = NSWindowController; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {
                connect = id; 
                disconnect = id; 
                goToWorldURL = id; 
                nextCommand = id; 
                previousCommand = id; 
                sendInputText = id; 
            }; 
            CLASS = MUConnectionWindowController; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                delegate = id; 
                inputView = NSTextView; 
                receivedTextView = MUDisplayTextView; 
                splitView = RBSplitView; 
            }; 
            SUPERCLASS = NSWindowController; 
        }, 
        {CLASS = MUDisplayTextView; LANGUAGE = ObjC; SUPERCLASS = MUTextView; }, 
        {CLASS = MUTextView; LANGUAGE = ObjC; SUPERCLASS = NSTextView; }, 
        {CLASS = NSObject; LANGUAGE = ObjC; }
    ); 
    IBVersion = 1; 
}